# MusicBox

1. build

```sh
yarn
yarn build
```

2. copy config

```sh
cp example.sqlite data.sqlite
```

3. run

```sh
node dist
```

4. usage

open http://127.0.0.1:12356
use `admin` / `admin` to login
